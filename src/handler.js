const bookshelfs = require('./books')
const { responseSuccess, responseError } = require('./response')

const getAllBookshelfsHandler = (req, h) => { }

const getSpecificBookshelfHandler = (req, h) => { }

const createBookshelfHandler = (req, h) => { }

const updateBookshelfHandler = (req, h) => { }

const deleteBookshelfHandler = (req, h) => { }

module.exports = {
    getAllBookshelfsHandler,
    getSpecificBookshelfHandler,
    createBookshelfHandler,
    updateBookshelfHandler,
    deleteBookshelfHandler
}